namespace MagnetPanic.Combat.Powerups
{
    /// <summary>
    /// Stable identifier for the three MVP powerups. Order matches the
    /// weight tuple stored in <see cref="PowerupWeights"/>.
    /// </summary>
    public enum PowerupId
    {
        None = 0,
        SlowTime = 1,
        OverloadPulse = 2,
        MagneticMine = 3,
    }
}
